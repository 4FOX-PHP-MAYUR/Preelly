/**
 * Identifiers for the Classifieds root category.
 * Used by getClassifiedCategories to scope results to classified-related categories only.
 */
const CLASSIFIED_ROOT_SLUGS = ['classifieds', 'classified', 'general-classifieds']

const CLASSIFIED_ROOT_NAMES = ['Classifieds', 'Classified']

module.exports = {
  CLASSIFIED_ROOT_SLUGS,
  CLASSIFIED_ROOT_NAMES,
}
