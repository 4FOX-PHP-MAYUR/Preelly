/**
 * Identifiers for the Property root category.
 * Used by getPropertyCategories to scope results to property-related categories only.
 */
const PROPERTY_ROOT_SLUGS = ['property', 'properties', 'real-estate', 'real-estate-property']

const PROPERTY_ROOT_NAMES = ['Property', 'Properties', 'Real Estate']

module.exports = {
  PROPERTY_ROOT_SLUGS,
  PROPERTY_ROOT_NAMES,
}
